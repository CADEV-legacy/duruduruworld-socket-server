export const ROUTE_URL = {
  api: '/api',
  health: {
    prefix: '/health',
  },
  auth: {
    prefix: '/auth',
  },
  user: {
    prefix: '/user',
  },
  socker: {
    prefix: '/socket',
  },
} as const;
